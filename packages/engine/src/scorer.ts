/**
 * Scorer — Calculates scores from KPI results.
 * Stub: implementation handled by Agent A.
 */

import type { KPIResult, KPIDefinition } from './types.js';

export class Scorer {
  scoreKPI(_definition: KPIDefinition, _output: string): KPIResult {
    // TODO: Implement automated + judge-based scoring (Agent A)
    throw new Error('Scorer.scoreKPI() not yet implemented');
  }
}
