/**
 * Suite Loader — Loads suite definitions from YAML or TypeScript.
 * Stub: implementation handled by Agent A.
 */

import type { SuiteDefinition } from './types.js';

export class SuiteLoader {
  async load(path: string): Promise<SuiteDefinition> {
    // TODO: Implement YAML + TS loading (Agent A)
    throw new Error(`SuiteLoader.load() not yet implemented for: ${path}`);
  }
}
